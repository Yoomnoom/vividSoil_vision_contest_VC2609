# VividSoul PRD

- 작성자: 이은정, 이고은
- 문서 버전: v3.1
- 작성일: 2026-07-30 (최초) / 2026-09-10 v2.0 / 2026-09-10 v3.0 / 2026-09-12 v3.1 갱신

---

## 1. 한 줄 소개

서울 자치구와 여행 날짜를 선택하면, 그날의 날씨에 따라 오늘의 맞춤 추천과 관심사 카테고리별 비짓서울 공식 콘텐츠를 4개 언어로 보여주는 반응형 웹앱.

---

## 2. 문제 정의

- 여행 계획을 세울 때 사람들은 보통 유명 관광지 위주의 정보만 얻게 되고, 관심사에 맞는 다양한 장소 정보는 찾기 어렵다.
- 여행 당일 날씨에 따라 실내/실외 일정을 조정해야 하는데, 날씨 정보와 여행지 추천이 따로 분리되어 있어 사용자가 직접 종합 판단을 해야 하는 번거로움이 있다.
- 사용자마다 여행에서 중요하게 여기는 것(음식, 역사, 자연, 쇼핑 등)이 다른데, 관심사와 무관하게 획일적인 추천만 받게 된다.
- 결과적으로 여행자는 "이 날씨에, 내가 관심 있는 분야 위주로, 이 지역에서 무엇을 하면 좋을지"를 한 번에 알려주는 서비스가 필요하다.

---

## 3. 핵심 기능

| 항목 | 내용 |
|---|---|
| 기능 | 오늘 날씨 맞춤 추천, 관심사(카테고리)별 비짓서울 공식 콘텐츠 추천, 서울 축제·행사 안내, 여행 참고 정보(환율, 현위치, 시간대별 날씨) 제공 |
| 입력 (사용자가 무엇을 넣으면) | 가고 싶은 서울 자치구 선택(또는 현위치), 여행 날짜 선택 |
| 출력 (AI/앱이 무엇을 돌려주면) | 선택한 지역·날짜의 날짜별/시간별 날씨 정보 → 오늘 날씨에 맞는 추천 6곳(카테고리 무관) + 관심사 카테고리 8종별 추천 각 6곳(결과 화면 탭으로 필터링) |

### 3-1. 관심사(카테고리)

문화관광, 쇼핑, 숙박, 역사관광, 음식, 자연관광, 체험관광, 축제/공연/행사 — 8종. **폼에서 사전 선택하지 않으며, 검색 시 항상 8종 전체에 대한 추천이 생성된다.** 사용자는 결과 화면 상단의 탭(전체 + 8종)을 클릭해 원하는 카테고리만 필터링해서 볼 수 있다.

### 3-2. 검색 결과 소스 (비짓서울 API 필드 기준)

장소 후보의 "존재 여부"와 "숨겨진 곳인지" 같은 기준은 AI가 판단하기 애매하므로, 결과에 노출되는 모든 장소는 비짓서울 API가 실제로 보유한 콘텐츠 중에서만 검색해 채운다. AI(Gemini)가 장소 이름을 새로 만들어내지 않고, 비짓서울 콘텐츠 필드를 그대로 활용한다.

| 용도 | 사용 필드 |
|---|---|
| 관심사 카테고리 필터 | `com_ctgry_sn` |
| 자치구(지역) 필터 | `contents/info` 응답의 `traffic.adres` 또는 `traffic.new_adres`에서 자치구명 추출 |
| 카드 표시 (이름/사진/설명) | `post_sj`(장소명), `main_img`(사진), `sumry`(요약 설명) |
| 상세페이지 링크 | `cid` |
| 다국어 | `lang_code_id`(언어별 콘텐츠 요청 시 사용) |

비짓서울 `contents/info` API는 `cid`를 기준으로 콘텐츠 상세 정보를 조회하며, 응답의 `traffic.adres` 또는 `traffic.new_adres`에서 서울 자치구명을 추출하여 지역 필터링에 사용한다.

검색 결과의 체감 지연을 최소화하기 위해 기존 CSV 데이터를 우선 활용하여 결과 화면을 즉시 표시한다. 비짓서울 API 조회는 CSV 기반 초기 결과 화면 표시를 차단하지 않도록 처리하며, 실제 API 데이터가 준비되면 CSV 기반 미리보기 결과를 최신 API 결과로 교체한다.

검색 시점마다 비짓서울 상세페이지 HTML을 크롤링하는 방식은 사용하지 않는다. 좌표(`traffic.map_position_x`/`traffic.map_position_y`) 기반 자치구 판별은 현재 범위에서 제외한다.

`cate_depth`, `relate_img`, `post_desc`, `tag`, `extra`(전화번호·홈페이지·이용시간·휴무일·편의시설 등), `traffic.new_zip_code`, `traffic.subway_info`, `schdul_info_bgnde`/`schdul_info_endde`, `multi_lang_list`, `tourist.guidance_service`, `tourist.safe_mng`는 현재 화면에서 사용하지 않으므로 제외한다. 향후 해당 정보를 활용하는 기능이 추가될 경우 사용 필드에 포함한다.

### 3-3. 사용자 플로우

1. 사용자가 웹앱에 접속한다.
2. (선택) 현위치 버튼으로 자동 지역 인식을 시도하거나, 직접 서울 자치구를 선택한다.
3. 여행 날짜를 선택한다 (오늘부터 16일 이내).
4. "추천 받기"를 누르면 앱이 해당 지역·날짜의 날씨 정보(일별 + 3시간 간격 시간대별)를 조회한다.
5. 결과 화면으로 자동 스크롤 이동하며, 첫째 날 날씨를 기준으로 (a) 오늘 날씨 맞춤 추천 6곳과 (b) 관심사 카테고리 8종별 추천이 함께 생성된다.
6. 사용자는 "오늘 날씨 맞춤 추천" 캐러셀을 좌우로 넘겨보고, 하단의 카테고리 탭을 눌러 원하는 관심사만 필터링해서 확인한다("전체" 탭은 카테고리별 3곳 미리보기, 개별 탭은 6곳+더보기).
7. 각 추천 카드에서 사진, 비짓서울 상세페이지(또는 구글 지도) 링크를 확인한다.
8. "축제/공연/행사" 탭을 선택하면 서울 축제·행사 카드도 함께 확인한다.
9. 언어 선택으로 한국어/영어/일본어/중국어 중 원하는 언어로 전체 화면(추천 텍스트 포함)을 볼 수 있다.

---

## 4. 외부 데이터 / API (구현 완료)

| 구분 | 사용 API | 비고 |
|---|---|---|
| 지오코딩 / 역지오코딩 | 카카오맵(Local) API | 지역명 → 위경도, 좌표 → 주소(현위치 기능). 지오코딩 결과는 30일 캐시 |
| 날씨 | Open-Meteo | API 키 불필요, 오늘부터 최대 16일 이내 예보만 제공. 일별 예보 + 3시간 간격 시간별 예보 |
| 추천 생성 (AI) | Gemini (`google-genai` SDK) | 장소를 새로 만들지 않고, 비짓서울 콘텐츠 후보(3-2 참고, `candidate_service.py`가 구성) 중에서 오늘 날씨에 맞는 곳을 골라 이유(why_this_weather)만 작성. 요청을 그룹으로 나눠 병렬 호출(무료 티어 분당 요청 제한 대응) |
| 장소 사진·상세페이지 | 비짓서울 API | 3-2의 필드(사진/설명/주소)를 그대로 사용. `contents/info`(공식 상세 조회 API)로 자치구를 판별해 자치구 우선 → 부족분은 서울 전체로 채움. 상세/후보 조회 결과 6시간 캐시 |
| 관광 콘텐츠(축제 등) | 비짓서울 API | 키워드 검색으로 서울 축제·행사 카드 제공, 서울 외 지역 콘텐츠는 지명 키워드로 필터링해 제외 |
| 환율 | 한국수출입은행 API | JPY/USD/CNY 환율 티커, 주말·공휴일은 최근 영업일 값으로 폴백 |

---

## 5. 완성 기준 (완료의 정의)

> "지역·날짜를 선택하면 오늘 날씨 맞춤 추천과 관심사 카테고리별로 분리된 비짓서울 공식 콘텐츠가 4개 언어로 추천된다."

- [x] 지역 선택 UI 동작 (서울 자치구 자동완성 + 대표 동네 표시 + 현위치 버튼)
- [x] 날짜 선택 UI 동작 (오늘부터 16일 이내)
- [x] 선택한 지역·날짜 기준 날씨 정보 표시 (일별 + 3시간 간격 시간대별, 접기/펼치기)
- [x] 오늘 날씨 맞춤 추천 6곳을 캐러셀로 표시
- [x] 관심사 카테고리별로 분리된 추천 섹션을 결과 화면 탭으로 필터링해 표시
- [x] 각 추천 장소에 사진 및 상세페이지/지도 링크 표시
- [x] 서울 축제·행사 카드 표시 ("축제/공연/행사" 탭 선택 시, 서울 외 지역 필터링)
- [x] 환율 티커, 현위치 UI 노출
- [x] 다국어 지원 완성 (한국어/영어/일본어/중국어 — UI뿐 아니라 AI 추천·날씨·비짓서울 콘텐츠까지 실제 번역)
- [x] 반응형 레이아웃(모바일/데스크톱) 적용
- [x] 응답 속도 최적화 (요청 그룹화·병렬화·캐싱 적용). **단, 비짓서울 상세 조회(자치구 판별)가 API 신뢰도 문제로 첫 조회(캐시 미스) 시 카테고리당 수십 초 걸릴 수 있음 — 캐시(6시간)가 채워진 이후 요청은 빠름. 7절 참고**
- [x] 검색 직후 실제 추천이 완성되기 전, 비짓서울 CSV 스냅샷으로 결과 화면(카테고리 카드)을 즉시 미리 채워 보여주고 실제 데이터로 자연스럽게 교체(체감 지연 개선)
- [x] 노출되는 모든 추천 장소가 비짓서울 API 콘텐츠로만 구성(AI가 존재 여부 불확실한 장소를 임의 생성하지 않음) — 3-2 참고. `candidate_service.py`가 카테고리·자치구로 실제 콘텐츠 후보를 구성하고, Gemini는 그 후보(cid) 중에서만 선택

---

## 6. 기술 스택 / 제약사항

- 프론트엔드: React + Vite
- 백엔드: Python 3.11 + Flask
- AI: Gemini — 반드시 `google-genai` SDK 사용 (구버전 `google-generativeai` 패키지 사용 금지)
- 서비스 지역: 서울 25개 자치구로 한정 (그 외 지역 입력 시 안내 후 차단)
- 여행 일정은 항상 당일(첫째 날) 기준으로만 추천 생성 (숙박일수 선택 없음)
- Gemini 무료 티어의 분당 요청 제한으로 인해 짧은 시간에 검색을 연속 실행하면 일시적으로 추천 생성이 실패할 수 있음
- 화면은 반응형으로 구현

---

## 7. 알려진 이슈

- 과거 구조(v3.0 이전): Gemini가 장소 이름을 먼저 생성한 뒤, 그 이름으로 비짓서울 API를 재검색해 사진·상세정보를 붙였다. 하지만 Gemini가 생성한 장소가 비짓서울 공식 콘텐츠 DB에 애초에 없는 경우가 많아(실측 매칭률 약 19%, 54곳 중 10곳), 재시도해도 해결되지 않는 매칭 누락이 구조적으로 발생했다.
- 결정(v3.1, 구현 완료): 3-2에 정리한 비짓서울 API 필드를 기준으로 처음부터 비짓서울이 실제로 보유한 콘텐츠 중에서만 후보를 구성하고(`backend/services/candidate_service.py`), Gemini는 그 후보(cid) 중에서 오늘 날씨에 맞는 곳을 고르고 이유만 작성한다(`backend/services/gemini_service.py`). 후보에 없는 장소를 고르면 결과에서 제외된다.
- 자치구 필터링 구현 메모: 비짓서울 목록 조회(`contents/list`)는 주소 필드를 주지 않아, 공식 상세 조회 API(`POST /api/v1/contents/info`, cid로 조회하며 응답의 `traffic.adres`/`traffic.new_adres`에 주소가 있음)를 항목별로 호출해 자치구를 판별한다. 이 상세 조회가 건당 2~3초로 느리고(비짓서울 쪽 특성) 500/연결 오류도 잦아, 응답 시간을 상한선 안에 묶기 위해 카테고리당 목록 1페이지(최대 50건)까지만 훑고, 그 안에서 선택한 자치구에 맞는 콘텐츠를 우선 채운 뒤 부족분은 같은 카테고리의 다른 서울 콘텐츠로 채운다(완전한 자치구 매칭을 포기하는 대신 응답 시간을 보장). 상세 조회 결과는 6시간 캐시되므로 같은 카테고리를 다시 검색하면(다른 자치구라도) 대부분 캐시로 빠르게 응답한다 — 즉 느린 구간은 카테고리당 최초 1회(캐시 미스)뿐이다.

---

## 8. 향후 고려사항 (MVP 이후 범위)

- 사용자 로그인/여행 기록 저장
- 서울 외 지역으로 서비스 범위 확대
- 다중 지역/다일정(숙박) 여행 코스 추천
- 사용자 리뷰·평점 기반 음식점 필터링
- Gemini 유료 플랜 전환 또는 추가 캐싱으로 응답 속도 추가 개선 및 요청 제한 리스크 해소

## 9. 비짓서울 API 기준

- 공식 API 문서:
  https://api.visitseoul.net/apiinfo/apilist/view/1?lang=ko

- 검색 결과에 노출되는 장소/콘텐츠는 위 비짓서울 공식 API가 제공하는 콘텐츠를 기준으로 한다.
- Gemini가 비짓서울에 존재하지 않는 장소를 새로 생성하여 추천하지 않는다.
- API의 실제 응답 필드와 PRD의 필드 정의가 다를 경우, 실제 API 응답을 우선 확인한 뒤 PRD를 수정한다.
- API에서 직접 제공되지 않는 데이터가 필요한 경우 임의로 구현하지 않고, 기존 데이터/프로젝트 내 수집 데이터의 출처를 먼저 확인한다.